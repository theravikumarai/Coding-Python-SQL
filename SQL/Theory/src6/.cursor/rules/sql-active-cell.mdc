---
description: >
  Currently active SQL notebook cell. Use this to scope edits to the correct
  cell block. Do NOT modify other cells unless explicitly asked.
globs:
  - "**/*.sql"
  - "**/*.sqlnb"
alwaysApply: false
---

# Active Cell Context

**File:** `r:\raviikrds\Professional\Git-Reposities\Learning\Courses\SQL\Harward-SQL\src6\injection\schema.sql`  
**Cell:** 4 of 4  
**Connection:** None  


## Current Cell SQL (Cell 4)

```sql
INSERT INTO `accounts` (`name`, `balance`)
VALUES
('Alice', 10),
('Bob', 20),
('Charlie', 30)
```

## Available Variables

_None defined_

## Surrounding Cells

**Previous cell (3):**
```sql
CREATE TABLE `accounts` (
    `id` INT AUTO_INCREMENT,
    `name` VARCHAR(16),
    `balance` INT,
    PRIMARY KEY(`id`)
)
```

_No next cell_

## Editing Rules

- Only edit **Cell 4** unless explicitly asked to change another cell
- Preserve `-- %%` cell boundary markers when editing
- Preserve `-- @var` and `-- @connection` directives
- Use `{{variableName}}` syntax to reference defined variables
